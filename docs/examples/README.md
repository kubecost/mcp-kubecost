# Example configurations

- [Network policies with Envoy Gateway and HTTPRoute](network-policies/README.md): Helm values and egress rules for isolating the MCP server.

Examples contain placeholder hostnames, namespaces and labels. Adapt them to your deployment before use.
