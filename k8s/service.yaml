apiVersion: v1
kind: Service
metadata:
  name: mcp-kubecost
  labels:
    app: mcp-kubecost
spec:
  selector:
    app: mcp-kubecost
  ports:
  - name: http
    protocol: TCP
    port: 3030
    targetPort: 3030
  type: ClusterIP
